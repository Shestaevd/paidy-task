services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: restaurant
    ports:
      - "5432:5432"
    volumes:
      - ./migrations:/docker-entrypoint-initdb.d
    networks:
      - net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d restaurant" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

#  app:
#    container_name: restaurant-server
#    image: paidy-restaurant-image:0.1
#    mem_limit: 1g
#    cpus: "1.0"
#    ports:
#      - "8080:8080"
#    environment:
##      this env is for test_container docker compose. If you want to start this project locally, change to "dev"
#      ENV: "e2e"
#    depends_on:
#      postgres:
#        condition: service_healthy
#    networks:
#      - net

networks:
  net:
    driver: bridge
